#!/bin/sh
set -eu

opsec --version | grep -q '^opsec 0\.1\.0$'
opsec --help | grep -q 'OPSEC Express'
opsec --once | grep -q 'OPSEC EXPRESS'
opsec --no-such-option >/dev/null 2>&1 && exit 1 || test "$?" -eq 1
