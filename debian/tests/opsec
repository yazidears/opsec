#!/bin/sh
set -eu

opsec --version | grep -q '^opsec 0\.1\.1$'
opsec --help | grep -q 'operation-security terminal meme'
opsec --once | grep -q 'OPERATION SECURITY'
opsec --no-such-option >/dev/null 2>&1 && exit 1 || test "$?" -eq 1
